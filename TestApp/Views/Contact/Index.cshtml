@model IEnumerable<СontactViewModel>
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row justify-content-between">
        <h2 class="col-md-4 col-sm-4 col-12 ">Contacts</h2>
        <a class="btn btn-light col-md-3 col-sm-3 col-12 m-auto mx-0" onclick="showPopup('@Url.Action("CreateOrEdit","Contact",null,Context.Request.Scheme)','Create contact')">Create New</a>
    </div>
</div>

<div id="view-all">
    @await Html.PartialAsync("_ViewMain",Model)
</div>
